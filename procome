#!/bin/bash
# Script wrapper para ejecutar PROCOME
# Permite ejecutar PROCOME desde cualquier ubicación

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"
python3 PROCOME_Arranque.py "$@"
